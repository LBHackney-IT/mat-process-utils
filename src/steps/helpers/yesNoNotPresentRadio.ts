import { yesNoRadios } from "./yesNoRadios";

export const tenantNotPresentRadio = {
  label: "Tenant not present",
  value: "tenant not present",
};

export const yesNoNotPresentRadios = [...yesNoRadios, tenantNotPresentRadio];
